# Translations folder

This folder is reserved for optional Qt `.qm` translation files.

The current experimental release also uses `i18n.py` for runtime UI text patching, so this folder may not contain compiled translation files yet. Keeping the folder documented avoids confusion for testers and maintainers.
